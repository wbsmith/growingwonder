<%- include('layout_head', { title: 'Liability Waiver' }) %>

<%- include('_nav') %>

<div class="program-page" style="max-width: 720px;">
  <h1 style="color: var(--green-dark); margin-bottom: 24px;"><%= (page.heroTitle) || 'Liability Waiver' %></h1>

  <% if (page.body) { %>
    <div class="program-description">
      <%- page.body %>
    </div>
  <% } else { %>
    <div style="text-align: center; color: var(--text-light); padding: 40px;">
      <p>Waiver content coming soon.</p>
    </div>
  <% } %>
</div>

<%- include('_footer') %>
<%- include('layout_foot') %>
